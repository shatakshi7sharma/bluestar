import * as React from 'react';

export interface IForgotPasswordProps {
}

export default function ForgotPassword (props: IForgotPasswordProps) {
  return (
    <div className="flex content-center items-center font-bold text-5xl h-screen w-screen">
      Ohh you can&apos;t forgot password before creating that page
    </div>
  );
}
