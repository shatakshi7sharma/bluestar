import * as React from 'react';

export interface ISignupProps {
}

export default function Signup (props: ISignupProps) {
  return (
    <div className="flex content-center items-center font-bold text-5xl h-screen w-screen">
      <div>This page i have to create.</div>
    </div>
  );
}
